#include<stdio.h>
#include<math.h>

int main(){
	int x, y;
	scanf("%d", &x);
	scanf("%d", &x);
	float pierwiastek;
	pierwiastek=sqrt((x*x)+(y*y));
	float cosinus;
	cosinus=x/pierwiastek;
	printf("%f", cosinus);
	return 0;
}
